version: '3'

services:
  sftp:
    networks:
      sftp-net:
    image: atmoz/sftp
    volumes:
        - /host/upload:/home/foo/upload
    ports:
        - "2222:22"
    command: foo:pass:1001
    volumes:
     - ./sftpdata:/home
  sftp_portal:
    networks:
      sftp-net:
        aliases:
          - start-dash
    build: .
    ports:
      - "8085:8085"
    container_name: sftp-start-dash
    restart: always

networks:
  sftp-net: